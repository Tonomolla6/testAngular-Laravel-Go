// import { Profile } from './profile.model';

export interface Discoteca {
  slug: string;
  name: string;
  company: string;
  events: string[];
  // createdAt: string;
  // updatedAt: string;
  // favorited: boolean;
  // favoritesCount: number;
  // author: Profile;
}
